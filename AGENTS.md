# Mecha Chameleon 작업 규칙

이 파일은 이 저장소에서 작업하는 사람과 에이전트의 첫 진입점이다. 코드, 씬, 프리팹, 패키지, 프로젝트 설정을 변경하기 전에 반드시 이 파일과 작업에 관련된 세부 문서를 읽는다.

## 문서 읽기 순서

1. `AGENTS.md`
2. 게임 규칙이나 UI 흐름을 바꿀 때 `GDD.md`
3. 코드, 네트워크, 씬, 데이터 흐름을 바꿀 때 `ARCHITECTURE.md`
4. 모델, 맵, 재질, 조명, UI 비주얼을 바꿀 때 `ART_STYLE.md`
5. 모든 구현 및 버그 수정 전에 `TEST.md`

여러 영역에 걸친 작업이면 해당 문서를 모두 읽는다.

## 문서의 권한

- 이 다섯 문서가 현재 승인된 제품 및 기술 기준선이다.
- 코드와 문서가 다르면 코드의 현재 동작을 먼저 확인한 뒤 차이를 보고한다. 의도 확인 없이 문서나 코드를 한쪽에 맞추지 않는다.
- 사용자의 요청이 문서와 충돌하면 **코드, 에셋, 씬, 설정을 변경하지 않는다.**
- 대신 충돌하는 문서 항목, 예상 영향, 가장 작은 문서 변경안과 구현안을 제안한다.
- 사용자가 문서 변경이나 예외 적용을 명시적으로 승인한 뒤에만 문서와 구현을 함께 변경한다.
- 단순한 버그 수정처럼 문서에 정의된 동작을 회복하는 작업은 별도 승인 없이 진행할 수 있다.
- 새 기능을 구현해 승인된 범위가 달라졌다면 같은 작업에서 관련 문서도 갱신한다.

## 현재 제품 목표

온라인 서비스 없이 같은 컴퓨터 또는 같은 LAN에서 실행되는 짧은 Mecha Chameleon 스타일 숨바꼭질 게임을 완성한다.

- Unity 6와 Netcode for GameObjects를 유지한다.
- 한 플레이어가 로컬 호스트이자 방장이고 다른 플레이어가 직접 접속한다.
- Firebase, Relay, Unity Multiplayer Services, 계정, 매치메이킹은 현재 목표에 필요하지 않다.
- 기존 Relay 코드는 제거가 승인되기 전까지 그대로 둘 수 있지만 새 UI의 기본 경로로 노출하지 않는다.
- 첫 완성 기준은 Home, Create Room, Join Room, Room, Options UI와 기존 라운드가 하나의 로컬 흐름으로 이어지는 것이다.

세부 범위와 게임 규칙은 `GDD.md`를 따른다.

## 구현 원칙

- 현재 코드를 먼저 읽고 기존 구조 안에서 가장 작은 변경을 한다.
- 한 씬인 `Assets/Scenes/Mvp.unity`를 기본으로 유지한다. 새 씬이 반드시 필요한 근거가 없으면 추가하지 않는다.
- 실제 게임 UI는 기존 패키지에 포함된 uGUI Canvas로 구현한다. `MvpHud.OnGUI`는 프로토타입이며 새 UI가 기능을 대체할 때 단계적으로 축소한다.
- 호스트가 라운드, 역할, 명중, 페인팅 승인 상태의 최종 권한을 가진다.
- 클라이언트가 보낸 역할, 명중, 타이머, 승패 결과를 그대로 신뢰하지 않는다.
- 매 프레임 실행되는 게임플레이 경로에서 불필요한 할당, LINQ, `Find` 호출을 추가하지 않는다.
- 현재 규모에서 사용하지 않을 범용 프레임워크, 서비스 로케이터, 이벤트 버스, 저장소 계층, 추상 팩토리를 만들지 않는다.
- 새 패키지나 온라인 의존성은 먼저 필요성과 대안을 제안하고 승인을 받은 뒤 추가한다.
- 생성된 에셋과 외부 에셋은 출처와 라이선스를 기록한다.

## Git 버전 관리와 브랜치

이 저장소는 개인 개발 프로젝트이므로 복잡한 GitFlow를 사용하지 않는다. 장기 브랜치는 `main` 하나만 유지하고, `develop`, `staging`, `production` 브랜치를 따로 만들지 않는다. Development, Staging, Production은 `DEPLOYMENT.md`의 빌드 환경과 커밋 SHA로 구분한다.

- 작업을 시작하기 전에 `git status --short --branch`로 현재 브랜치와 기존 변경을 확인한다.
- `main`은 실행 가능하고 검증된 기준선으로 유지한다. 문서 오타처럼 동작에 영향을 주지 않는 작은 수정만 `main`에서 직접 처리할 수 있다.
- 코드, 씬, 프리팹, 패키지, 프로젝트 설정 또는 게임 동작을 바꾸는 작업은 `codex/<type>-<short-name>` 형식의 짧은 브랜치에서 진행한다. 예: `codex/fix-shot-effect`, `codex/feat-online-room`.
- 한 브랜치에는 하나의 기능이나 버그 수정만 포함한다. 관련 없는 정리, 리팩터링, 포맷 변경을 함께 넣지 않는다.
- 기존의 커밋되지 않은 변경은 사용자의 작업으로 간주한다. 임의로 되돌리거나 stash, 이동, 커밋하지 않으며 새 작업과 섞지 않는다. 작업 진행이 불가능할 때만 사용자에게 확인한다.
- 여러 에이전트나 작업을 동시에 실행할 때만 작업별 Git worktree와 브랜치를 사용한다. 하나의 작업만 진행할 때 worktree를 만들지 않는다.
- 같은 씬, 프리팹, `.meta`, `Packages/`, `ProjectSettings/` 파일은 한 번에 한 작업자만 수정한다. 병렬 작업 전에 파일 소유 범위를 나눈다.
- 스테이징은 `git add -A` 대신 이번 작업에서 변경한 파일만 명시적으로 추가한다. 비밀값, `Library/`, `Logs/`, 빌드 결과물과 로컬 사용자 설정은 커밋하지 않는다.
- 커밋 하나는 되돌리거나 설명할 수 있는 하나의 동작 단위로 만든다. `misc`, `AI changes`, `updates` 같은 메시지를 사용하지 않는다.
- 브랜치의 임시 커밋은 허용하지만 `main`에 병합할 때 불필요한 WIP 커밋은 squash한다. 의미 있는 독립 변경이 여러 개일 때만 커밋을 나누어 유지한다.
- 에이전트는 사용자가 명시적으로 요청하기 전에는 commit, push, merge, tag를 실행하지 않는다. 완료 보고에는 변경 파일, 테스트 결과, 실행하지 못한 검증과 남은 위험을 포함한다.
- `git reset --hard`, 강제 checkout, 강제 push처럼 작업을 잃을 수 있는 명령은 사용자가 해당 작업을 명시적으로 승인하지 않으면 실행하지 않는다.
- 병합 전에는 `TEST.md`에 정의된 영향 범위 테스트를 실행한다. 네트워크, 인증, 씬, 프리팹 또는 배포 변경은 전체 관련 테스트와 수동 플레이 확인 후 병합한다.
- 개인 개발에서는 모든 작업에 Pull Request나 다른 사람의 승인을 요구하지 않는다. 큰 변경의 diff 기록, 원격 검토 또는 고위험 변경 검증이 필요할 때만 Pull Request를 사용한다.
- 검증이 끝난 브랜치는 `main`에 병합한 뒤 삭제한다. 출시 가능한 동일 커밋을 Staging에서 확인한 후에만 `v<major>.<minor>.<patch>` 태그를 만들고 Production에 사용한다.

## 보호 대상

- `Assets/Scenes/Mvp.unity`가 현재 실행 및 빌드 씬이다.
- `Assets/Prefabs/ChameleonPlayer.prefab`이 네트워크 플레이어 프리팹이다.
- `Assets/NetworkPrefabs.asset`이 NGO 등록 목록이다.
- `Mecha Chameleon/Build MVP Scene` 메뉴는 씬과 관련 에셋을 재생성하고 덮어쓸 수 있다. 사용자가 명시적으로 요청하지 않으면 실행하지 않는다.
- 이미 열려 있는 사용자의 Unity Editor를 임의로 종료하지 않는다.
- 테스트 중 UDP `7778` 포트 충돌이 나면 다른 프로세스 사용 여부와 NGO 종료 상태를 확인한 뒤 실패 테스트만 한 번 재실행한다.

## 완료 조건

변경이 완료되려면 다음을 만족해야 한다.

- 요청한 사용자 흐름이 실제 플레이에서 동작한다.
- 네트워크 상태는 호스트와 클라이언트에서 일관된다.
- 관련 EditMode 및 PlayMode 테스트를 추가하거나 갱신했다.
- 영향받는 자동 테스트가 통과했다.
- UI 변경은 Home, Room, 게임 HUD, Options의 상태 전환과 화면 크기별 겹침을 수동 확인했다.
- 관련 문서가 현재 동작과 일치한다.
- 실행하지 못한 검증이나 남은 위험을 결과 보고에 명시한다.

## 문서 지도

- `GDD.md`: 제품 목표, 화면 흐름, 라운드 규칙, 조작, 범위
- `ARCHITECTURE.md`: 코드 소유권, NGO 권한, 로컬 방 검색, UI 구조, 데이터 흐름
- `ART_STYLE.md`: 집 내부 테마, 캐릭터와 소품 스케일, 재질, 조명, UI 비주얼
- `TEST.md`: 자동 테스트, 수동 멀티플레이 시나리오, 기능별 통과 기준

